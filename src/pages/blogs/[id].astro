---
import Layout from "@/layouts/Layout.astro";
import BlogsCard from "../../components/BlogsCard";
const { id } = Astro.params;
export const prerender = false;

console.log(id);
---

<Layout>
  <!-- <main class="main-area fix">
    <section
      class="blog-details-area section-py-120"
      data-aos="fade-up"
      data-aos-delay="200"
    >
      <div class="container">
        <div class="row">
          <div class="col-xl-9 col-lg-8">
            <h3
              class="title"
              style="padding-bottom: 20px; font-size: 36px;"
              data-aos="fade-right"
              data-aos-delay="300"
            >
              {blog.title}
            </h3>
            <div class="blog__post-meta">
              <ul class="list-wrap">
                <li><i class="flaticon-calendar"></i>{blog.published_date}</li>
                <li>
                  <i class="flaticon-user-1"></i> by <a href="#">Admin</a>
                </li>
                <li><i class="flaticon-clock"></i> 5 Min Read</li>
                <li><i class="far fa-comment-alt"></i> 05 Comments</li>
              </ul>
            </div>
            <div class="blog__details-wrapper">
              <div class="blog__details-thumb">
                <img src={blog.thumbnail} alt="img" />
              </div>
              <div class="blog__details-content">
                <p>{blog.description}</p>
                <ul class="about__info-list list-wrap">
                  {
                    blog.tags.map((tag: string) => (
                      <li class="about__info-list-item">
                        <i class="flaticon-angle-right" />
                        <p class="content">{tag}</p>
                      </li>
                    ))
                  }
                </ul>
              </div>
            </div>
          </div>
          <div
            class="col-xl-3 col-lg-4"
            data-aos="fade-left"
            data-aos-delay="400"
          >
            <aside class="blog-sidebar">
              <div class="blog-widget">
                <h4 class="widget-title">Categories</h4>
                <div class="shop-cat-list">
                  <ul class="list-wrap">
                    <li>
                      <a href="#"
                        ><i class="flaticon-angle-right"></i>{blog.category}</a
                      >
                    </li>
                  </ul>
                </div>
              </div>
              <div class="blog-widget">
                <h4 class="widget-title">Tags</h4>
                <div class="tagcloud">
                  {blog.tags.map((tag: string) => <a href="#">{tag}</a>)}
                </div>
              </div>
            </aside>
          </div>
        </div>
      </div>
    </section>
  </main> -->
  <BlogsCard client:only="react" id={id} />
</Layout>
